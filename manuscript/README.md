# Manuscript Source

This directory contains diff-friendly manuscript source.

- `sections/`: chapter and section Markdown files plus metadata.
- `tables/`: structured table sources.
- `images/`: figure references and caption metadata.

Only approved source should be assembled into final delivery artifacts.


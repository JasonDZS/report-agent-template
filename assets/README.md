# Assets

Asset workflow:

1. Create a brief under `assets/briefs/`.
2. Create or refine the generation prompt under `assets/prompts/`.
3. Store generated files under `assets/generated/`.
4. Reference approved assets from `manuscript/images/`.


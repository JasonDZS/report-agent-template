# Sources

Place original source documents under `sources/raw/`.

Place extracted, task-sized excerpts under `sources/excerpts/`.
Agents should normally receive excerpts through a context pack instead of reading every raw source file.

